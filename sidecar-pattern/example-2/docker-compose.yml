services:
  app:
    build: ./app
    expose:
      - "8080"
  
  nginx-sidecar:
    image: nginx:alpine
    ports:
      - "8443:443"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/certs:/certs
    depends_on:
      - app